{{ node.name }}

execution
  {{ node.role }} · {{ node.harness }}{% if node.model %}/{{ node.model }}{% endif %} · {% if node.execution %}{{ node.execution }}{% else %}unassigned{% endif %}
  lifecycle     {{ node.status }}
  judge         {{ node.judgePolicy }}
  attempt       {{ node.attempt }}
  reports to    {% if node.reviewBy %}{{ node.reviewBy }}{% else %}{{ node.completion }}{% endif %}
{% if gates %}needs attention
{% for gate in gates %}  - {{ gate.reason }}
{% endfor %}{% endif %}
purpose         {{ node.purpose }}
goal            {{ node.goal }}
expected output {{ node.expectedOutput }}
{% if node.successCriteria %}success criteria
{% for criterion in node.successCriteria %}  - {{ criterion }}
{% endfor %}{% endif %}
